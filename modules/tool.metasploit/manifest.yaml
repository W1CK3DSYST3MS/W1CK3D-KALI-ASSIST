module_id: tool.metasploit
name: "Metasploit Framework — Exploitation"
version: "1.0.0"
type: tool
os_profile: kali
requires:
  base_api: ">=1.0"
  modules: []
provides:
  tool: metasploit
  flows: [setup_db, search_use, configure_run, auxiliary, sessions_meterpreter, post, payloads, msfvenom, handler, workflow_db, maintenance]
  glossary_terms: [framework, module_types, exploit, payload, auxiliary, post, meterpreter, rhosts_lhost, staged_stageless, reverse_bind, session, job, handler, datastore, workspace, msfvenom, msfdb]
content:
  tool: tool.yaml
  glossary: glossary.yaml
source: "authored; verified against msfconsole/msfvenom help + Rapid7/Offensive Security docs"
license: "project-proprietary lesson text; Metasploit Framework is BSD (referenced, not bundled)"
checksum: ""
