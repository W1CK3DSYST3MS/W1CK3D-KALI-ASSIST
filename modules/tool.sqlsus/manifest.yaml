module_id: tool.sqlsus
name: "sqlsus — MySQL injection to interactive takeover"
version: "1.0.0"
type: tool
os_profile: kali
requires:
  base_api: ">=1.0"
  modules: []
provides:
  tool: sqlsus
  flows: [guided]
content:
  tool: tool.yaml
  glossary: glossary.yaml
source: "authored; verified against installed sqlsus --help + /usr/share/doc/sqlsus/README + a generated example config (Kali 2026.3, sqlsus 0.7.2)"
license: "MIT (this repo); sqlsus is GPL (referenced, not bundled)"
checksum: ""
