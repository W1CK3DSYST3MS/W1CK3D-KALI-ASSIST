module_id: tool.photon
name: "Photon — fast web crawler for OSINT"
version: "1.0.0"
type: tool
os_profile: kali
requires:
  base_api: ">=1.0"
  modules: []
provides:
  tool: photon
  flows: [guided, tune, secrets_dns]
  glossary_terms: [api_key, subdomain_enum]
content:
  tool: tool.yaml
  glossary: glossary.yaml
source: "authored; verified against installed Photon v1.2.2 --help (-u/-l/-t/-d/-o/--wayback/--keys/--dns/--clone/-e)"
license: "MIT-licensed project text (this repo); Photon is GPL (referenced, not bundled)"
checksum: ""
